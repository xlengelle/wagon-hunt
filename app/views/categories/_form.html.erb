 <%= simple_form_for @category do |f| %>
        <%= f.input :name, placeholder: "Entertainment", label: "- Renseignez la catégorie -" %>

        <%= f.button :submit, "Valider", class: "btn btn-primary" %>

      <% end %>
